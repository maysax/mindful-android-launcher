<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_pause"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="co.minium.launcher3.pause.PauseActivity">

    <include layout="@layout/siempo_toolbar"/>

      <TextView
        android:id="@+id/txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:layout_marginBottom="30dp"
        android:layout_centerHorizontal="true"
        tools:text="30 minute" />

    <com.jesusm.holocircleseekbar.lib.HoloCircleSeekBar
        android:layout_centerInParent="true"
        android:id="@+id/seekbar"
        android:layout_width="285dp"
        android:layout_height="290dp"
        app:max="60"
        app:init_position="15"
        app:pointer_color="@color/pointer"
        app:pointer_halo_color="@color/pointerHalo"
        app:pointer_size="20dp"
        app:title_color="@color/text.primary"
        app:title_size="90sp"
        app:subTitle_color="@color/text.secondary"
        app:subTitle_size="20sp"
        app:show_title="true"
        app:text_size="24sp"
        app:text_color="@color/text.primary"
        app:thumb_image="@drawable/control_dial_handle"
        app:thumb_size="32dp"
        app:wheel_active_color="@color/pauseSeekBarActive"
        app:wheel_unactive_color="@color/pauseSeekBarInactive"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="60dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true">

    </LinearLayout>

    <TextView
        android:text="Turn on Tempo to batch notifications at set intervals"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:layout_marginStart="47.3dp"
        android:layout_marginEnd="46.1dp"
        android:paddingTop="15.8dp"
        android:textSize="12sp"
        android:textColor="#8a000000"
        android:layout_marginTop="28dp"
        android:paddingBottom="69.3dp"
        android:gravity="center"
        android:id="@+id/text_status" />

    <LinearLayout
        android:orientation="horizontal"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="20dp"
       >


        <Button
            android:text="OFF"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:id="@+id/btnOff"
            android:background="@null"
            android:textSize="14sp"
            android:textColor="#332d6d"
            android:layout_weight="1" />

        <Button
            android:text="ON"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:id="@+id/btnOn"
            android:background="@null"
            android:textSize="14sp"
            android:textColor="#4d332d6d"
            android:layout_weight="1" />
    </LinearLayout>


</RelativeLayout>
